// `/perf` probe (plan §6/M6). See `perf-page.tsx` for the module header
// covering scope and how it's reached (`App.tsx`'s `?perf` route, mirroring
// `m2-slice.tsx`'s `?m2`).
export { PerfPage } from './perf-page';
